// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyAezd58i2NTzf6bA_IrL-W03jbllIQI_T4",
    authDomain: "store-image-bbce1.firebaseapp.com",
    databaseURL: "https://store-image-bbce1-default-rtdb.firebaseio.com",
    projectId: "store-image-bbce1",
    storageBucket: "store-image-bbce1.appspot.com",
    messagingSenderId: "987428208813",
    appId: "1:987428208813:web:c01aeaa006458aebce8624",
    measurementId: "G-P382WR7EDJ"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
